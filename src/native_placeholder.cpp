// Native build placeholder.
// This file keeps the native PlatformIO environment buildable without
// pulling ESP32-specific application sources into the host build.

#ifndef ARDUINO
int main() {
	return 0;
}
#endif
