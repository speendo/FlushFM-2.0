# US-0027c: Hook-Wiring Invariants and Tests

> **Status:** Done | **Priority:** Medium | **Created:** 2026-04-25

## User Story
As a **maintainer**, I want **to enforce static hook wiring assumptions without runtime ambiguity**, so that **hook setup is deterministic and guard-removal safe**.

## Acceptance Criteria
- [x] Hook wiring is deterministic and statically modeled
- [x] Runtime guard branches that contradict static invariants are removed from production paths where proven safe
- [x] Graceful degradation is preserved despite guard cleanup

## Notes
This story depends on US-0027b and completes the US-0027 module-boundary work.

## Related
- Guidelines: `requirements/guidelines/software-architecture.md`
- Guidelines: `requirements/guidelines/testing.md`
- Stories: `US-0027`
- Stories: `US-0027b`

### Implementation
- Removed null-callback guard (`if (!transitionInvoker || !timeoutHook) return false;`) from `setComponentTransitionHooks(ComponentID, ...)` in supervisor.cpp.
- All 4 component classes pass non-null callbacks via `registerWithController()`. Defensive check was dead code in production.
