# US-0029: Rename SystemController Class to Supervisor

> **Status:** Done | **Priority:** High | **Created:** 2026-05-08

## User Story
As a **maintainer**, I want **the SystemController class to be renamed to Supervisor throughout the codebase**, so that **naming is consistent with the architectural guidelines and user story wording**.

## Acceptance Criteria
- [x] Class `SystemController` is renamed to `Supervisor` in `src/state_machine/system_controller.h`
- [x] All method implementations in `src/state_machine/system_controller.cpp` are updated to use `Supervisor` class scope
- [x] Log source constant `kLogSource` in system_controller.cpp is updated to reflect `"Supervisor"`
- [x] All component registration methods use `Supervisor` in declarations and definitions
- [x] All includes and forward declarations reference `Supervisor` where appropriate
- [x] All test files referencing `SystemController` are updated to use `Supervisor` (test_system_components_contract, test_state_transition_flow)
- [x] All source files that instantiate or use `SystemController` are updated (main.cpp, component files)
- [x] Debug and production builds succeed without errors
- [x] All native unit tests pass (`pio test -e native`)

## Notes
- This refactoring aligns user story wording and code naming with the architectural guidelines in `requirements/guidelines/state-management.md`, which consistently use "Supervisor" as the architectural term.
- User stories US-0025a, US-0020, US-0027, US-0027a, US-0008, US-0026b, and US-0011 have already been updated with "Supervisor" wording.
- Files were renamed: `system_controller.h` → `supervisor.h`, `system_controller.cpp` → `supervisor.cpp`
- All includes throughout the codebase were updated to reference the new file names
- Component registry and all public APIs remain functionally unchanged; this is a naming refactor only.

## Related
- Guidelines: `requirements/guidelines/state-management.md`
- Guidelines: `requirements/guidelines/modularity.md`
- Stories: `US-0025a`, `US-0027`, `US-0027a`
