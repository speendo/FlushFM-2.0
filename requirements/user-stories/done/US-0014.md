# US-0014: Component Architecture – Extend ISystemComponent with State Methods and Compile-Time Safe Timeouts

> **Status:** Done  
> **Priority:** High  
> **Created:** 2026-03-29

---

## User Story

As a **component developer**,  
I want **explicit setOFF, setIDLE, setSTREAMING, setERROR methods in ISystemComponent**,  
so that **the compiler forces me to implement all required state transitions**.

---

## Acceptance Criteria

- [x] Update `src/components/composition/system_components.h`: ISystemComponent interface extended
- [x] Add virtual methods returning timeout in milliseconds: `uint32_t setOFF(uint32_t transitionId)`, `setIDLE(uint32_t transitionId)`, `setSTREAMING(uint32_t transitionId)`, `setERROR(uint32_t transitionId)`
- [x] Add timeout hook method: `void onTransitionTimeout(uint32_t transitionId)`
- [x] `setXXX()` timeout return is static component-owned configuration for that target state
- [x] All existing concrete components (BoardInfo, WiFi, AudioRuntime, CLI) updated with implementations
- [x] No compilation errors on all components
- [x] At least one component (BoardInfoComponent) has real, complete implementations
- [x] Timeouts are state-specific and compile-time enforced through required `setXXX()` methods
- [x] Missing overrides are compile-time errors via pure-virtual interface; concrete classes validated as non-abstract

---

## Test Strategy

**Write tests first (TDD per `testing.md`):**
- [x] Unit test: Compile-check that concrete implementations must override all `setXXX()` methods → missing override = compiler error
- [x] Unit test: `setOFF()`, `setIDLE()`, `setSTREAMING()`, `setERROR()` each return `uint32_t` timeout value (test stub implementations)
- [x] Unit test: `onTransitionTimeout(uint32_t transitionId)` can be called repeatedly and safely
- [x] Unit test: All four existing components (BoardInfo, WiFi, AudioRuntime, CLI) have all required implementations (no pure virtual left)
- [x] Integration check: Debug and production builds pass with updated interface and implementations

**Corner Cases & Runtime Safety:**
- [x] Unit test: `setXXX(0)` (zero transitionId) is handled correctly (valid but edge case)
- [x] Unit test: `setXXX(UINT32_MAX)` (max transitionId) does not cause overflow
- [x] Unit test: Timeout return value of 0 is accepted (immediate completion expected)
- [x] Unit test: Timeout return value of UINT32_MAX is accepted (very long timeout)
- [x] Unit test: `onTransitionTimeout()` called twice with same ID doesn't crash component
- [x] Unit test: Component in safe state after `onTransitionTimeout()` is verifiable (no dangling pointers, no resource leaks)
- [x] Unit test: Multiple components' `setXXX()` calls do not interfere (state isolation)
- [x] No memory leaks when calling `setXXX()` repeatedly
- [x] No deadlock or infinite loop if component synchronously calls back during `setXXX()`

Tests run on host and validate interface contract before component development begins.

---

## Notes

Each component knows its own timing best. Timeout values are provided by each `setXXX()` method.
No controller fallback and no separate timeout lookup API.
After every `setXXX()` call, the component must emit exactly one completion callback (`Completed` or `Failed`).
On timeout, the SystemController calls `onTransitionTimeout(transitionId)`; the component must move to a safe internal state and then report `Failed` with an optional reason.

### Completion Notes

- Interface extended in `src/components/composition/system_components.h` with four state methods and timeout hook.
- All concrete components updated in `src/components/composition/system_components.cpp`.
- BoardInfoComponent serves as complete synchronous reference implementation.
- New contract tests added in `test/test_system_components_contract/test_main.cpp`.
- Verification completed with:
	- `~/.platformio/penv/bin/platformio test --environment native`
	- `~/.platformio/penv/bin/platformio run --environment debug`
	- `~/.platformio/penv/bin/platformio run --environment production`

---

## Related

- Guidelines: `requirements/guidelines/modularity.md` (Component interfaces and lifecycle)
- Stories: `US-0013` (types depend on this), `US-0015`, `US-0016` (orchestration)
