# US-0004e: Decouple CLI Output from Command Logic

> **Status:** To Do  
> **Priority:** Medium  
> **Created:** 2026-03-28

---

## User Story

As a **developer**,  
I want **CLI command handling and CLI output rendering to be separated**,  
so that **command code stays readable, testable, and easier to maintain**.

---

## Acceptance Criteria

- [ ] CLI command execution no longer writes output text directly inside command decision branches
- [ ] A dedicated output layer/module is introduced for CLI messages (info, error, help, command result)
- [ ] Command handlers return structured results (status + optional payload/message key) instead of printing directly
- [ ] `cli.cpp` keeps orchestration responsibilities (read input, dispatch command, pass result to output layer)
- [ ] Existing command behavior and user-visible CLI output remain functionally equivalent
- [ ] Build succeeds in debug and production environments
- [ ] At least one representative command path is covered by a unit or component-level test without relying on `Serial`

---

## Notes

- Keep migration incremental: start with helper output functions, then extract renderer and result model.
- Align log severity routing with existing debug guideline policy.
- Suggested implementation sequence:
	1. Introduce command-result structs and route one command path through them.
	2. Extract CLI output rendering into a dedicated module.
	3. Migrate remaining commands and add one test path without `Serial` dependency.

---

## Related

- Guidelines: `requirements/guidelines/modularity.md` (Lifecycle Contract, Interaction)
- Guidelines: `requirements/guidelines/debug.md` (log levels and output behavior)
- Guidelines: `requirements/guidelines/testing.md`
- Stories: `US-0004b`
